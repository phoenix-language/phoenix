# Rust target directory
/target

# macOS files
.DS_Store

# Environment files
.env
.env.local

# Phoenix bytecode files
*.phx0
*.pxi

# Phoenix project build output (phoenix.toml [build].dir, default "build")
**/build/

# Compiler build driver sources — Rust module, not project artifacts
!source/phx-compiler/src/build/
!source/phx-compiler/src/build/**
