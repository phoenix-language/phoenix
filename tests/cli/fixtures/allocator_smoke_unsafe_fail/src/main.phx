#import std::core::memory::allocator::{Allocator, Layout, VmHeapAllocator};

main :: () => {
    var alloc: VmHeapAllocator = VmHeapAllocator {};
    const layout: Layout = Layout { size: 8u, align: 1u };
    const ptr: *mut u8 = alloc.alloc(layout);
    alloc.dealloc(ptr, layout);
};
