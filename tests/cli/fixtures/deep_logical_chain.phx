main :: () => {
    const a: s32 = 1;
    const b: s32 = 2;
    const c: s32 = 3;
    const d: s32 = 4;
    const e: s32 = 5;
    const f: s32 = 6;
    const ok: bool = (a < b) && (b < c) && (c < d) && (d < e) && (e < f) && (f < 10);
    const _ = if ok { 1 } else { 0 };
};
