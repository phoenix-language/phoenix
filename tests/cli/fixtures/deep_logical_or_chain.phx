main :: () => {
    const a: s32 = 1;
    const b: s32 = 2;
    const c: s32 = 3;
    const d: s32 = 4;
    const e: s32 = 5;
    const f: s32 = 6;
    const hit: bool = (a > 10) || (b > 10) || (c > 10) || (d > 10) || (e > 10) || (f < 10);
    const _ = if hit { 1 } else { 0 };
};
