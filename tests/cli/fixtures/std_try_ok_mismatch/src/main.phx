#import std::core::result::{Result, Ok};

read_a :: () => Result<s32, s32> {
    Ok(1)
};

read_b :: () => Result<bool, s32> {
    const x = read_a()?;
    Ok(x)
};

main :: () => {
    const _ = read_b();
};
