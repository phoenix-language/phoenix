#import cmp::PartialEq;

mod cmp;

#[derive(PartialEq)]
Container :: <t> struct {
    len: u32,
};

main :: () => {
    const a = Container :: <s32> { len: 1u };
    const b = Container :: <s32> { len: 1u };
    const _: bool = a.eq(&b);
};
