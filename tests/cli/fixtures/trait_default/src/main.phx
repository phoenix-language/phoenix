Counter :: struct {
    n: s32,
};

Zero :: trait {
    zero :: () => Self {
        Counter { n: 0 }
    };
};

Counter :: impl :: Zero { };

main :: () => {
    const c: Counter = Counter::zero();
    const n: s32 = c.n;
    const _ = n;
};
