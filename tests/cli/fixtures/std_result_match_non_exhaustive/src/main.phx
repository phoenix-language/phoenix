#import std::core::result::{Result, Ok, Err};

Config :: struct {
    value: s32,
};

AppError :: struct {
    code: s32,
};

make_ok :: () => Result<Config, AppError> {
    Ok(Config { value: 0 })
};

main :: () => {
    match make_ok() {
        Ok(c) => { const _ = c.value; };
    };
};
