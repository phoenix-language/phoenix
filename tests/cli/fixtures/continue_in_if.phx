// continue inside if within loop (regression for loop back-edge placement).

main :: () => {
  var i: s32 = 0;
  loop {
    i = i + 1;
    if 3 > (i) {
      continue;
    }
    break;
  };
  const _ = i;
};
