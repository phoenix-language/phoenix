#import std::core::iter::{Range};
#import std::core::option::{Option, Some, None};

main :: () => {
    var sum: s32 = 0;
    for x in Range { start: 0, end: 3 } {
        sum = sum + x;
    };
    const _ = sum;
};
