[project]
name = "app_dep"
version = "0.1.0"
description = "App depending on math lib"
type = "bin"
module_src = "src"
bundle_std = false

[dependencies]
math = { path = "../math_lib" }

[build]
dir = "build"
