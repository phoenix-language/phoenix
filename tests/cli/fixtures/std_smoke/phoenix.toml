[project]
name = "std_smoke"
version = "0.1.0"
description = "Smoke test consumer for repo std package"
type = "bin"
module_src = "src"

[dependencies]
std = { path = "../../../../std" }

[build]
dir = "build"
