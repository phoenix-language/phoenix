#import std::core::alloc::{alloc_bytes, dealloc_bytes};

main :: () => {
    unsafe {
        const buf: *mut u8 = alloc_bytes(4u);
        dealloc_bytes(buf, 4u);
        dealloc_bytes(buf, 4u);
    };
};
