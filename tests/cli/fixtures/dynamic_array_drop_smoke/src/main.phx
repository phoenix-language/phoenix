#import std::collections::dynamic_array::DynamicArray;
#import std::core::memory::allocator::VmHeapAllocator;

main :: () => {
    {
        var arr: DynamicArray<s32> = DynamicArray :: <s32>::empty(VmHeapAllocator {});
        arr.push(1);
        const _ = arr.len();
    };
};
