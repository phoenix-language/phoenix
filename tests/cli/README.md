# CLI tests

End-to-end tests for the `phx` binary.

| Script | What it does |
|--------|----------------|
| [`check.sh`](check.sh) | Builds `phx`, runs `phx check` on [`fixtures/sample.phx`](fixtures/sample.phx) |

From the repo root:

```bash
tests/cli/check.sh
# or
just test-cli
```
