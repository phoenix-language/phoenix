#import std::core::cmp::PartialEq;
#import std::core::fmt::Debug;

#[derive(PartialEq, Debug)]
#[lang_item(name = "Result", kind = "enum")]
pub Result :: <ok, err> enum {
    Ok(ok),
    Err(err),
};
