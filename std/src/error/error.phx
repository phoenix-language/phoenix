pub IoError :: struct {
    code: s32,
};

pub ParseError :: struct {
    code: s32,
};

pub ThreadError :: struct {
    code: s32,
};

pub GeneralError :: struct {
    code: s32,
};

pub Error :: enum {
    Io(IoError),
    Parse(ParseError),
    Thread(ThreadError),
    General(GeneralError),
};
