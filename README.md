# Phoenix-Lang (Experimental Alpha)

Phoenix is a statically typed, no-GC language that compiles to portable bytecode and runs on a VM—aimed at systems-style control over memory, with the runtime visible in the type system instead of hidden behind a big OS-thread or GC model.