//! `phx` command-line driver (binary crate).
//!
//! Will invoke [`phx_compiler::compile_source`] / [`phx_compiler::check_file`] for `check` and
//! run workflows once CLI flags are implemented. Today this is a workspace link placeholder.

fn main() {}
