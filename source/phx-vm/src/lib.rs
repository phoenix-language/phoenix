//! Phoenix VM — bytecode loader and stack interpreter (scaffold).
//!
//! Will execute verified [`phx_bytecode`] `PHX0` images with a single-process stack machine for MVP.

/// Workspace link stub; remove when the crate exports real API.
pub const WORKSPACE: () = ();
