//! Phoenix bytecode — `PHX0` encode/decode, opcode definitions, and verification (scaffold).
//!
//! Portable output of the compiler; consumed by [`phx_vm`] after the verifier pass.

/// Workspace link stub; remove when the crate exports real API.
pub const WORKSPACE: () = ();
